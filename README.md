# Elevate-Container-demo Parent Project

* Install Docker.

* From the project directory, start docker-compose: `docker-compose up -d`. This will take some time to set up your containers the first time you run it.